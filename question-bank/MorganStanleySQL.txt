SET NULL "NULL";
SET FEEDBACK OFF;
SET ECHO OFF;
SET HEADING OFF;
SET WRAP OFF;
SET LINESIZE 10000;
SET TAB OFF;
SET PAGES 0;
SET DEFINE OFF;
SET SERVEROUTPUT ON;
/*
Enter your query here.
Please append a semicolon ";" at the end of the query
*/


select COMPANY.NAME from COMPANY
where COMPANY.ID IN
(select DISTINCT COMPANY_ID from (select AVG(SALARY.SALARY),COMPANY_ID from SALARY
GROUP BY COMPANY_ID
HAVING AVG(SALARY.SALARY) >= 40000) D);

exit;